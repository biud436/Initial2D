# 7단계: RPG 프레임워크 3 — 대화창과 윈도우 UI

**권장 모델**: Claude Opus 5 — 스킨 조립, 줄바꿈, 타자 효과 등 시각적 반복 조정이 중심이다. 코루틴 연동의 어려운 부분은 6단계 산출물을 그대로 사용한다.

> 목표: System 윈도우 스킨으로 대화창을 그리고, 한 글자씩 출력되는 메시지, 얼굴 그래픽, 선택지를 만든다.
> 전부 Lua(`scripts/rpg/window.lua`, `message.lua`)다. 나인 슬라이스 같은 개념을 C++에 넣지 않고, `Sprite.SetRect`로 스킨 조각을 잘라 Lua에서 조립한다.

## 재료 확인

- **윈도우 스킨**: RTP `System/System.png` (160x80, 4단계에서 투명 변환). 배경, 프레임, 선택 커서, 페이지 넘김 화살표, 글자색 팔레트가 한 장에 들어 있다. 세부 분할 좌표는 구현 시 실물 이미지와 EasyRPG Player의 Window 구현을 교차 확인해 `specs.lua`에 확정 기록한다.
- **한글 폰트**: `resources/fonts/hangul.fnt` (BMFont, 나눔고딕, lineHeight 32, 한글 음절 2453자)가 이미 동작한다. 단, 코드포인트 허용 범위가 라틴1과 한글 음절뿐이므로(`src/Font.cpp:233`) 한자와 특수문자는 안 나온다 — 데모 텍스트는 이 범위 안에서 작성한다.
- **텍스트 측정**: 1단계의 `GetTextWidth` 바인딩이 선행 조건이다 (줄바꿈 계산에 필수).
- **해상도 주의**: 스킨과 폰트의 배율이 어긋나기 쉽다. RTP 스킨은 320x240 기준이고 hangul.fnt는 32px 기준이므로, 논리 해상도를 640x480으로 잡고 스킨을 2배로 그리면 폰트와 궁합이 맞는다. 데모 초기값으로 삼고 실물로 조정한다.

## 핵심 설계

### window.lua — 범용 창 (RPG 프레임워크 안의 공용품)

- 스킨 조각을 잘라 창을 조립: 네 모서리, 네 변(반복 또는 늘이기), 배경(늘이기)
- 창 열기와 닫기 애니메이션 (세로 스케일 또는 투명도)
- 내용은 콜백으로 그린다 (메시지 창, 선택지 창, 이후 메뉴 창이 공유)

### message.lua — 대화창

- 화면 하단 고정 크기 창, `ctx.message(text, opts)`로 호출 (6단계의 코루틴 인터페이스 구현체)
- **한 글자씩 출력** (타자 효과): UTF-8 문자 단위로 진행, 결정키로 즉시 전체 표시, 다시 결정키로 닫기
- **자동 줄바꿈**: `GetTextWidth`로 어절 단위 폭을 재서 접는다 (한국어는 어절 단위 접기면 충분, 초과 시 글자 단위)
- 3줄 초과 시 페이지 넘김 (스킨의 대기 화살표 표시)
- `opts.face = { file, index }`: FaceSet 48x48을 좌측에 표시, 텍스트 영역을 그만큼 밀기
- `opts.name`: 이름 표시 (본문 첫 줄 또는 작은 이름 창, 구현하며 택일)

### choice.lua — 선택지

- `ctx.choice({ ... })`: 메시지 창 우하단 또는 별도 창에 항목 나열, 스킨 커서 사각형으로 선택 표시, 위아래 키와 결정키, 취소키(X 또는 Esc) 처리
- 반환값은 선택 인덱스 (취소 시 nil 또는 지정 인덱스)

## 작업 항목

- [x] `specs.lua`에 System 스킨 분할 좌표 확정 (실물 검증)
- [x] `scripts/rpg/window.lua`: 스킨 조립, 열기와 닫기
- [x] `scripts/rpg/message.lua`: 타자 효과, 줄바꿈, 페이지, 얼굴, 코루틴 대기
- [x] `scripts/rpg/choice.lua`: 선택지
- [x] 6단계 `ctx.message`와 `ctx.choice`의 print 스텁을 실물로 교체
- [x] 대화 효과음 (글자 출력음, 커서음) — 자작 WAV (`tools/generate_ui_assets.py`)
- [x] 데모: NPC 대화에 얼굴과 선택지가 나오는 장면

## 완료 기준

- [x] "NPC에게 말을 건다 → 얼굴 있는 대화창이 한 글자씩 출력된다 → 선택지를 고른다 → 분기한 대사가 나온다"가 끊김 없이 동작한다.
- [x] 긴 한국어 문장이 창 폭에 맞게 자연스럽게 줄바꿈된다.
- [x] 대화 중에는 플레이어와 NPC가 멈추고, 닫히면 재개된다.
- [x] C++ 수정은 1단계에서 이미 넣은 범용 기능만 사용한다. (예외 하나: 아래 구현 메모 4번 —
      `Sprite.SetRect` 바인딩이 처음부터 동작하지 않던 것을 고쳤다. 새 기능이 아니라 버그 수정이다.)

## 구현 메모 (2026-08-18)

1. **스킨 분할은 실물로 확정했다.** `resources/rtp/System/System.png`의 알파 채널을 픽셀 단위로
   떠서 확인했고, 결과는 `scripts/rpg/specs.lua`의 `M.window`에 있다. 계획 문서의 추측과 달리
   스크롤 화살표는 (128,0) 블록이 아니라 **테두리 블록 한가운데**에 있다 (위 40,8,16,8 / 아래 40,16,16,8).
   (128,0) 32x32는 전투 UI용이라 대화창은 쓰지 않는다.
2. **늘이기 대신 반복.** 엔진의 스프라이트 배율은 가로세로 같은 값 하나뿐이라(SDL2 어댑터의
   XFORM 분해) 변과 바탕을 늘일 수 없다. 그래서 `window.lua`는 조각을 반복해 채우고, 남는
   자투리는 그 크기짜리 스프라이트를 따로 만들어 소스를 자른다. 바탕은 원본을 세로로 4등분해
   띠마다 해당 부분만 반복한다 — 32픽셀마다 그라데이션이 끊기는 것을 줄이려는 절충이다.
3. **여닫기 애니메이션도 배율이 아니라 기하로.** 창 높이를 openness만큼 줄여 위아래 가운데에서
   자라게 한다. 세로만 늘이는 배율이 없어서이기도 하고, 이쪽이 픽셀도 깨지지 않는다.
4. **`Sprite.SetRect` 바인딩 수정 (C++).** 나인 슬라이스는 "텍스처의 임의 위치를 잘라 그리기"가
   있어야 성립하는데, 이 바인딩은 테이블 인자만 받으면서(그래서 `scripts/image.lua`의 네 인자
   호출은 조용히 무시됐다) width와 height를 y 변수에 덮어써 읽고 있었다. 두 형태를 모두 받고
   좌표를 제대로 채우도록 고쳤다 (`src/lua_sprite.cpp`, 검증은 `tests/lua/cases/sprite_sheet_test.lua`).
   RPG 전용 기능이 아니라 범용 API의 버그다.
5. **RTP 없이도 돌아간다.** 스킨과 얼굴은 커밋 금지라, 같은 규격의 플레이스홀더를 만들어 커밋했다
   (`tools/generate_windowskin.py` → `resources/ui/window.png`, `tools/generate_faceset.py` →
   `resources/faces/placeholder.png`). 효과음도 자작 WAV다(`tools/generate_ui_assets.py`).
   데모와 맵 정의는 RTP가 있으면 그쪽을 자동으로 고른다.
6. **글자색 팔레트는 아직 장식이다.** 스킨의 견본 20색은 좌표만 기록해 두었다 — 엔진의
   `DrawText`에 색 지정이 없어서 지금은 쓸 수 없다. 필요해지면 1단계 성격의 작업으로 따로 넣는다.
7. **줄바꿈 여유(inkMargin).** `GetTextWidth`는 글자의 진행 폭(advance) 합이라, 획이 그보다 조금
   더 나가는 글자가 줄 끝에 오면 테두리에 닿아 보인다. 창과 줄바꿈 폭에 배율 x 2픽셀의 여유를 둔다.

### 검수 결과

- Lua 단위 테스트 147건 추가 (`rpg_window_test`, `rpg_choice_test`, `rpg_message_test`, `sprite_sheet_test` 보강) — 전체 885건 통과.
- 엔진 씬 테스트 `rpg_dialogue_scene` 추가: 창 좌표를 Lua가 stdout으로 알려 주고 러너가 그
  사각형의 픽셀을 검사한다 (테두리, 바탕, 글자, 얼굴, 이름 창, 선택 커서). 같은 씬을
  `INITIAL2D_DLG_MODE=arrow`로 한 번 더 돌려 대기 화살표까지 본다. 골든 스크린샷 1장.
- 안드로이드 APK 디버그 빌드 성공 (C++ 변경이 JNI 소스 목록에 이미 있는 파일 하나뿐이고,
  새 리소스는 `android/prepare_assets.sh`가 통째로 복사한다).
- `tests/run_all.sh` 전체 통과 (C++ 단위, Lua 단위, 픽셀 검증, 골든, 브리지 18건, RTP 1923건).
- 눈 확인: 플레이스홀더 스킨과 RTP System 스킨 양쪽으로 데모를 자동 시연 모드로 돌려 스크린샷 확인.

## 의존 관계

- 선행: 1단계(텍스트 측정, 해상도 설정), 4단계(System과 FaceSet 변환), 6단계(코루틴 인터페이스)
- 후행: 8단계
